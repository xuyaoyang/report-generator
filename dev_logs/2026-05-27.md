# 2026-05-27 开发日志

## 完成事项
- [x] **合格证紧凑排版**: 新建 `_compact_certificates()`，行高从 603 → 360 twips（atLeast），段落行距 280，删除合格证组间空段落（16个），保留含分页符的空段落
- [x] **删除空白页**: 新建 `_remove_blank_pages()`，将连续 2+ 空段落折叠为 1 个（移除 46 个），保留含分页符的段落
- [x] **外观检测表行距压缩**: 新建 `_compact_visual_tables()`，第 3/4/5/8 行保持 567 twips，其余行 280 twips
- [x] **TOC 独立分页**: 新建 `_add_toc_page_break()`，在第一个合格证前插入分页符，确保目录单独成页
- [x] **修复分页符被误删**: `_compact_certificates` 增加 `_has_page_break()` 检查，不再删除含分页符的空段落
- [x] 新增辅助函数：`_elem_tag()`, `_elem_text()`, `_has_page_break()`

## 模版路径
`products/isolation_bearing/template_prepared.docx`

## Bug 修复（同日）

- [x] **VML 图片段落被误删**: `_remove_blank_pages` 和 `_compact_certificates` 将无 `w:t` 文本的 VML 图片段落（印章、营业执照、材质书扫描件）当作"空段落"删除。新增 `_has_image()` 辅助函数，检查 `w:drawing`/`w:pict`/`w:object` 元素。修复后：
  - 封面印章保留
  - 营业执照页保留
  - 8 页材质书全部保留
  - 产品合格证印章保留
  - 外观检测报告印章/签名保留
- [x] **空白页（合格证/力学报告后）**: 模板中部分段落同时存在 `w:br type="page"`（分页符）和 `w:sectPr`（分节符，类型 NEW_PAGE），两者各自触发换页导致空白页。新增 `_fix_section_breaks()` 移除含分节符段落中的冗余分页符；新增 `_has_section_break()` 保护含分节符的段落不被 `_compact_certificates` 和 `_remove_blank_pages` 误删

- [x] **格式处理经验文档**: 创建 [docs/Word模板格式处理经验.md](docs/Word模板格式处理经验.md)，汇总 8 类格式问题和解决方案，供新增产品类型时参考

## 待办事项
- [ ] 材质单图片管理模块（上传、标签、自动匹配）
- [ ] Table 6（力学汇总表）的型号行数也随型号数量动态调整
- [ ] 打包为 .exe 分发
